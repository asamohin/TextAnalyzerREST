spring:
  datasource:
    url: jdbc:h2:tcp://localhost/~/warn_test
    username:
    password:
config:
  rootdir: D:\\modeluper
#настройки логирования
logging:
  level:
    root: INFO
  pattern:
    console: "%d{yyyy-MM-dd HH:mm:ss} - %msg%n"
    file: "%d{yyyy-MM-dd HH:mm:ss} [%thread] %-5level %logger{36} - %msg%n"
  file: /log/classifier.log  